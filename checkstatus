include "shell.h"

	int
	_checkstatus(char *exec_func, char **path)
{
	int s;
	waitpid(pid, &s, 0);
	free(exec_func);
	if (WIFEXITED(s))
	{
		return (WEXITSTATUS(s));
	}
	else
	{
		free(path);
		free(exec_func);
		exit(EXIT_FAILURE);
	}
}
